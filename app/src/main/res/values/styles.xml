<resources>
    <!-- Estilo de tema para o aplicativo -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/delfisBackground</item>
        <item name="colorPrimaryDark">@color/delfisBackground</item>
        <item name="colorAccent">#03DAC5</item>
        <item name="android:navigationBarColor">@color/delfisBackground</item>
    </style>

    <!-- Estilo personalizado para botões -->
    <style name="CustomButtonStyle">
        <item name="android:background">@drawable/button_background</item>
        <item name="android:textColor">#FFFFFF</item>
        <item name="android:padding">16dp</item>
    </style>
</resources>
